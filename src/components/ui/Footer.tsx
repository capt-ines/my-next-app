import React from "react";
import { StyledFooter } from "@/styles/footer.styled";

const Footer = () => {
  return (
    <StyledFooter>
      <h3>footer</h3>
    </StyledFooter>
  );
};

export default Footer;
